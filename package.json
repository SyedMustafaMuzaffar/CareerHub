{
    "name": "placement-hub-root",
    "version": "1.0.0",
    "description": "Root package to manage Placement Hub frontend and backend",
    "scripts": {
        "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
        "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\""
    },
    "devDependencies": {
        "concurrently": "^8.0.0"
    }
}